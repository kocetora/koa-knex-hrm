'use strict';
const { addUser } = require('./addUser');
const { getUser } = require('./getUser');
const { getUserEmails } = require('./getUserEmails');

module.exports = {
  addUser,
  getUser,
  getUserEmails,
};
